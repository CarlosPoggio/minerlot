{
  "name": "minerlot-logs-service",
  "version": "1.0.0",
  "private": true,
  "description": "Streams bitcoind's debug.log over WebSocket for the Minerlot dashboard. Reads the log file directly (read-only volume mount) — no Docker socket access.",
  "main": "server.js",
  "dependencies": {
    "ws": "^8.18.0"
  }
}
